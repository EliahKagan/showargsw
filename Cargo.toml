[package]
name = "showargsw"
version = "0.1.0"
edition = "2021"

[dependencies.windows]
version = "0.58.0"
features = [
    "Win32_UI_WindowsAndMessaging",
]

[build-dependencies]
winresource = "0.1.17"

[package.metadata.winresource]
manifest = "app.manifest"
